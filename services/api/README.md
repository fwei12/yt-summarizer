# API Service

Placeholder for the HTTP API.

Planned endpoints:

- `POST /jobs` creates a summarization job.
- `GET /jobs/:id` returns job status and metadata.
- `GET /jobs/:id/summary` returns a completed summary.
- `GET /health` reports service health.
